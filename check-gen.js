const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log('Products in General:', await prisma.product.count({ where: { category: { name: 'General' } } })); const p = await prisma.product.findMany({ select: { category: { select: { name: true } } }, take: 2000 }); console.log('Sample categories:', Array.from(new Set(p.map(x => x.category ? x.category.name : 'NULL')))); } main().catch(console.error).finally(() => prisma.$disconnect());
