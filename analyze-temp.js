const fs = require('fs'); const cheerio = require('cheerio'); const html = fs.readFileSync('temp-output.html', 'utf-8'); const $ = cheerio.load(html); console.log('Sidebar links:'); $(.sidebar-tabs a).each((i, el) => console.log($(el).attr('href'))); console.log('\nCard styles:'); $(.cables-card).each((i, el) => { console.log('Card ' + i, 'Classes:', $(el).attr('class'), 'Style:', $(el).attr('style')); });
