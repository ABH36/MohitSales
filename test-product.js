const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const p = await prisma.product.findUnique({ where: { slug: 'industries/cables-by-application/mobility-infrastructure' } }); console.log(p ? 'Is Product' : 'Not Product'); } main().catch(console.error).finally(() => prisma.$disconnect());
