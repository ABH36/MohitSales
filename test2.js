const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const p = await prisma.product.findUnique({ where: { slug: 'industries/cables-by-application/mobility-infrastructure' } }); console.log('CreatedAt:', p.createdAt.getTime(), 'UpdatedAt:', p.updatedAt.getTime(), 'HasImage:', p.imageSrc); } main().catch(console.error).finally(() => prisma.$disconnect());
